# DocuSense — Research Paper Analysis RAG System
# Install:  pip install -r requirements.txt

# ==================== Core ====================
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
loguru>=0.7.2
tqdm>=4.66.0

# ==================== API & Web ====================
fastapi>=0.115.0              # REST API framework
uvicorn[standard]>=0.30.0     # ASGI server
python-multipart>=0.0.9       # Required by FastAPI for file uploads

# ==================== Authentication ====================
bcrypt>=4.1.0                 # Password hashing
PyJWT>=2.8.0                  # Access tokens
email-validator>=2.1.0        # Pydantic EmailStr validation

# ==================== LLM Providers ====================
ollama>=0.1.0                 # Local LLM inference (answer generation)
google-generativeai>=0.3.0    # Gemini API (query rewriting, vision)

# ==================== Document Processing ====================
markitdown>=0.0.1a2           # Universal doc -> Markdown converter
pypdf2>=3.0.1                 # Fallback PDF parser
pdfplumber>=0.10.3            # Alternative PDF parser (better tables)
python-docx>=1.1.0            # Word document parser
pillow>=10.0.0                # Image manipulation
pytesseract>=0.3.10           # OCR fallback for images
tiktoken>=0.5.2               # Token counting for chunking
chardet>=5.2.0                # File encoding detection
python-magic-bin>=0.4.14; sys_platform == "win32"   # File type detection (Windows)

# ==================== Embeddings & Vector Store ====================
sentence-transformers>=5.0.0  # Local embeddings + cross-encoder reranker
qdrant-client>=1.17.0         # Qdrant vector database
torch>=2.4.0                  # Backend for sentence-transformers
numpy>=1.24.0

# ==================== Hybrid Retrieval ====================
rank-bm25>=0.2.2              # BM25 keyword search

# ==================== Evaluation ====================
# Required for scripts/benchmark.py. Without rouge-score the ROUGE columns are
# reported as null rather than 0.0, so a missing install cannot be mistaken for
# a genuine score of zero.
rouge-score>=0.1.2            # ROUGE metrics for answer quality
# bert-score is optional and not installed by default: it pulls in a second
# transformer model for a metric the benchmark does not currently rely on.
# Install it to populate the BERTScore columns.

# QASPER is read straight from its released JSON, so HuggingFace `datasets` is
# not needed. It was listed here and never imported.

# ==================== Development ====================
pytest>=7.4.3
pytest-cov>=4.1.0
pytest-asyncio>=0.21.1
httpx>=0.27.0                 # Required by fastapi.testclient
black>=23.12.0
# Pinned, not floating. A linter's rule set grows between releases, so an
# unpinned `ruff>=...` means CI can fail on code nobody touched: CI resolved
# 0.16.4 while this venv had 0.15.0, and the newer default rule set flagged
# nine pre-existing __all__ blocks. Local and CI now run the same tool, and
# upgrading is a deliberate commit that fixes any new findings with it.
ruff==0.15.0
mypy>=1.7.1
